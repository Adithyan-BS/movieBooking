<!DOCTYPE html>
<html lang="en">

  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Bootstrap 5 Login form Page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    
  </head>

  <body>
    <form id="formData" action="/SignupData" method="post">
    <div class="vh-100 d-flex justify-content-center align-items-center">
      <div class="container">
        <div class="row d-flex justify-content-center">
          <div class="col-12 col-md-8 col-lg-6">
            <div class="card bg-white">
              <div class="card-body p-5">
                <form class="mb-3 mt-md-4">
                  <h2 class="fw-bold mb-2 text-uppercase ">Signup</h2>
                  <p class=" mb-5">Please enter Email Id</p>
                  <div class="mb-3">
                    <label for="email" class="form-label ">Email address</label>
                    <input type="email" class="form-control" id="Email"name="email" placeholder="name@example.com">
                  </div>
                  <div class="mb-3">
                    <label for="password" class="form-label" >Password</label>
                    <input type="password" class="form-control" id="password" name="password" placeholder="*******">

                    <label for="password" class="form-label" >Confirm Password</label>
                    <input type="password" class="form-control" id="confirmpassword" name="confirmPassword" placeholder="*******">
                  </div>
                  <div class="d-grid">
                    <button class="btn btn-outline-dark" type="submit" onclick="return submitData()">Signup</button>
                  </div>
                </form>
                <div>
                 
                </div>

              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
</form>

  </body>

</html>
<script>  
  function submitData(){
    let firstPassword = document.getElementById('password').value; 
    let secondPassword=document.getElementById("confirmpassword").value
    let password=firstPassword.toString();
    let conformPassword=secondPassword.toString();
    if(password === secondPassword){
      return true
    }else{
      alert('Password doesnot match')
      clearInput()
      return false
    }
  }

  function clearInput(){
    let form=document.getElementById('formData')
    form.reset()
  }
  
  
</script>
